[project]
name = "skills-hub-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "skill-eval",
    "pytest>=8.0",
    "pytest-xdist>=3.5",
]

# The site builder is not a runtime dependency — run it with uvx:
#   uvx --from git+https://github.com/harvard-lil/skills-hub#subdirectory=packages/skills-hub-builder \
#       skills-hub-builder build
[tool.uv.sources]
skill-eval = { git = "https://github.com/harvard-lil/skills-hub", subdirectory = "packages/skill-eval" }
